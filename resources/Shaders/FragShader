#version 460 core

layout (location = 0) out vec4 color;

uniform vec4 colour;
uniform vec2 light_pos;

in vec4 out_color;
in vec4 out_pos;

void main()
{
	float intensity = 10.0 / length(out_pos.xy - light_pos);
	color = out_color * intensity;
}